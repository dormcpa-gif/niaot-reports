# URL of the backend API (FastAPI). Set this in Netlify's site environment
# variables once the Python backend is hosted somewhere reachable
# (Netlify Functions only support Node.js, so the backend can't live here).
VITE_API_BASE=http://localhost:8000
